import Foundation

/// GrabKit is a debug-only UI graph, element picker, and transport starter
/// for SwiftUI/UIKit/AppKit apps.
///
/// Add `.grabRoot()` near your app root and `.grab(...)` on meaningful views.
public enum GrabKit {
    public static let version = "0.1.0"
}
